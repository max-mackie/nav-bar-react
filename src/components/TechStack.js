import React, { useState, useRef, useEffect } from "react";

const TechStack = () => {
  return (
    <section id="tech-stack" className="section">
      <h2>tech stack</h2>
      {/* display this in a diamond shape instead of logos in a grid */}
    </section>
  );
};

export { TechStack };
